const DEFAULT_API_LOCALHOST: string = 'http://localhost:80/api/v1';

export const home: string = `${DEFAULT_API_LOCALHOST}/`;
export const registration: string = `${DEFAULT_API_LOCALHOST}/auth`;
export const session: string = `${DEFAULT_API_LOCALHOST}/sign_in`;